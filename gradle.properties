# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
# This is required to provide enough memory for the Minecraft decompilation process.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
changelog=Now using FTB Utilities Nicknames and name format\nAdd Messages for died tamed animals (default: off)\n\nUpdated JDA to 4.0\nAdded default config command /tps\nFix some messages being ignored when command (like /tps with many dimensions) sent too many\n